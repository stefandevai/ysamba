import type { FrameFormValues } from './types';

export const defaultFrame : FrameFormValues = {
  key: -1,
  type: 'tile',
  sprite_type: 'single',
  angle: 'parallel',
  frame: -1,
  game_id: null,
};
